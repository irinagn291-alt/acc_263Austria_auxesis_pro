import UIKit

/// Role: the only radius language. 14pt surfaces, 6pt chips. Never a hard edge.
public enum AuxRadius {
    public static let surface: CGFloat = 14
    public static let chip: CGFloat = 6
}
